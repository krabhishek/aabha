# eslint-plugin-aabha

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build eslint-plugin-aabha` to build the library.

## Running unit tests

Run `nx test eslint-plugin-aabha` to execute the unit tests via [Vitest](https://vitest.dev/).
